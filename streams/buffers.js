// Representação de um espaço na memória de um computador,
// utilizado para transitar dados de maneira muito rápida

// Salvar/ler da memória de maneira muito PERFORMÁTICA

// O buffer lê o dado de maneira binária

const buf = Buffer.from("hello")

console.log(buf.toJSON())